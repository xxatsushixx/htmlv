{
  "name": "htmlv",
  "version": "1.0.0",
  "description": "HTML for Video (htmlv) — compiler, timeline IR, and browser player",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "htmlv": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc && node scripts/chmod-cli.js",
    "start": "node dist/index.js",
    "serve": "node dist/cli.js serve examples/showcase.htmlv",
    "test": "npm run build && node dist/test/run.js"
  },
  "author": "htmlv contributors",
  "license": "MIT",
  "files": [
    "dist",
    "player",
    "examples",
    "README.md"
  ],
  "devDependencies": {
    "@types/node": "^22.7.4",
    "typescript": "^5.6.0"
  }
}
