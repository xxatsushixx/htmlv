#!/usr/bin/env node
/**
 * htmlv CLI — build and serve Timeline IR + browser player.
 */
export declare function build(input: string, outDir: string): void;
export declare function serve(input: string, port: number): void;
export declare function main(argv?: string[]): void;
